config BR2_PACKAGE_PG_AUDIT
	bool "pg-audit"
	depends on BR2_PACKAGE_POSTGRESQL
	help
	  The PostgreSQL Audit Extension (pgAudit) provides detailed session and/or object audit logging via
	  the standard PostgreSQL logging facility.
